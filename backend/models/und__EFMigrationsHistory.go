// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// EFMigrationsHistory is an object representing the database table.
type EFMigrationsHistory struct {
	MigrationId    string `boil:"MigrationId" json:"MigrationId" toml:"MigrationId" yaml:"MigrationId"`
	ProductVersion string `boil:"ProductVersion" json:"ProductVersion" toml:"ProductVersion" yaml:"ProductVersion"`

	R *eFMigrationsHistoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L eFMigrationsHistoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EFMigrationsHistoryColumns = struct {
	MigrationId    string
	ProductVersion string
}{
	MigrationId:    "MigrationId",
	ProductVersion: "ProductVersion",
}

var EFMigrationsHistoryTableColumns = struct {
	MigrationId    string
	ProductVersion string
}{
	MigrationId:    "__EFMigrationsHistory.MigrationId",
	ProductVersion: "__EFMigrationsHistory.ProductVersion",
}

// Generated where

var EFMigrationsHistoryWhere = struct {
	MigrationId    whereHelperstring
	ProductVersion whereHelperstring
}{
	MigrationId:    whereHelperstring{field: "[dbo].[__EFMigrationsHistory].[MigrationId]"},
	ProductVersion: whereHelperstring{field: "[dbo].[__EFMigrationsHistory].[ProductVersion]"},
}

// EFMigrationsHistoryRels is where relationship names are stored.
var EFMigrationsHistoryRels = struct {
}{}

// eFMigrationsHistoryR is where relationships are stored.
type eFMigrationsHistoryR struct {
}

// NewStruct creates a new relationship struct
func (*eFMigrationsHistoryR) NewStruct() *eFMigrationsHistoryR {
	return &eFMigrationsHistoryR{}
}

// eFMigrationsHistoryL is where Load methods for each relationship are stored.
type eFMigrationsHistoryL struct{}

var (
	eFMigrationsHistoryAllColumns            = []string{"MigrationId", "ProductVersion"}
	eFMigrationsHistoryColumnsWithAuto       = []string{}
	eFMigrationsHistoryColumnsWithoutDefault = []string{"MigrationId", "ProductVersion"}
	eFMigrationsHistoryColumnsWithDefault    = []string{}
	eFMigrationsHistoryPrimaryKeyColumns     = []string{"MigrationId"}
)

type (
	// EFMigrationsHistorySlice is an alias for a slice of pointers to EFMigrationsHistory.
	// This should almost always be used instead of []EFMigrationsHistory.
	EFMigrationsHistorySlice []*EFMigrationsHistory
	// EFMigrationsHistoryHook is the signature for custom EFMigrationsHistory hook methods
	EFMigrationsHistoryHook func(context.Context, boil.ContextExecutor, *EFMigrationsHistory) error

	eFMigrationsHistoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	eFMigrationsHistoryType                 = reflect.TypeOf(&EFMigrationsHistory{})
	eFMigrationsHistoryMapping              = queries.MakeStructMapping(eFMigrationsHistoryType)
	eFMigrationsHistoryPrimaryKeyMapping, _ = queries.BindMapping(eFMigrationsHistoryType, eFMigrationsHistoryMapping, eFMigrationsHistoryPrimaryKeyColumns)
	eFMigrationsHistoryInsertCacheMut       sync.RWMutex
	eFMigrationsHistoryInsertCache          = make(map[string]insertCache)
	eFMigrationsHistoryUpdateCacheMut       sync.RWMutex
	eFMigrationsHistoryUpdateCache          = make(map[string]updateCache)
	eFMigrationsHistoryUpsertCacheMut       sync.RWMutex
	eFMigrationsHistoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var eFMigrationsHistoryBeforeInsertHooks []EFMigrationsHistoryHook
var eFMigrationsHistoryBeforeUpdateHooks []EFMigrationsHistoryHook
var eFMigrationsHistoryBeforeDeleteHooks []EFMigrationsHistoryHook
var eFMigrationsHistoryBeforeUpsertHooks []EFMigrationsHistoryHook

var eFMigrationsHistoryAfterInsertHooks []EFMigrationsHistoryHook
var eFMigrationsHistoryAfterSelectHooks []EFMigrationsHistoryHook
var eFMigrationsHistoryAfterUpdateHooks []EFMigrationsHistoryHook
var eFMigrationsHistoryAfterDeleteHooks []EFMigrationsHistoryHook
var eFMigrationsHistoryAfterUpsertHooks []EFMigrationsHistoryHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *EFMigrationsHistory) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eFMigrationsHistoryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *EFMigrationsHistory) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eFMigrationsHistoryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *EFMigrationsHistory) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eFMigrationsHistoryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *EFMigrationsHistory) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eFMigrationsHistoryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *EFMigrationsHistory) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eFMigrationsHistoryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *EFMigrationsHistory) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eFMigrationsHistoryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *EFMigrationsHistory) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eFMigrationsHistoryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *EFMigrationsHistory) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eFMigrationsHistoryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *EFMigrationsHistory) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eFMigrationsHistoryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddEFMigrationsHistoryHook registers your hook function for all future operations.
func AddEFMigrationsHistoryHook(hookPoint boil.HookPoint, eFMigrationsHistoryHook EFMigrationsHistoryHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		eFMigrationsHistoryBeforeInsertHooks = append(eFMigrationsHistoryBeforeInsertHooks, eFMigrationsHistoryHook)
	case boil.BeforeUpdateHook:
		eFMigrationsHistoryBeforeUpdateHooks = append(eFMigrationsHistoryBeforeUpdateHooks, eFMigrationsHistoryHook)
	case boil.BeforeDeleteHook:
		eFMigrationsHistoryBeforeDeleteHooks = append(eFMigrationsHistoryBeforeDeleteHooks, eFMigrationsHistoryHook)
	case boil.BeforeUpsertHook:
		eFMigrationsHistoryBeforeUpsertHooks = append(eFMigrationsHistoryBeforeUpsertHooks, eFMigrationsHistoryHook)
	case boil.AfterInsertHook:
		eFMigrationsHistoryAfterInsertHooks = append(eFMigrationsHistoryAfterInsertHooks, eFMigrationsHistoryHook)
	case boil.AfterSelectHook:
		eFMigrationsHistoryAfterSelectHooks = append(eFMigrationsHistoryAfterSelectHooks, eFMigrationsHistoryHook)
	case boil.AfterUpdateHook:
		eFMigrationsHistoryAfterUpdateHooks = append(eFMigrationsHistoryAfterUpdateHooks, eFMigrationsHistoryHook)
	case boil.AfterDeleteHook:
		eFMigrationsHistoryAfterDeleteHooks = append(eFMigrationsHistoryAfterDeleteHooks, eFMigrationsHistoryHook)
	case boil.AfterUpsertHook:
		eFMigrationsHistoryAfterUpsertHooks = append(eFMigrationsHistoryAfterUpsertHooks, eFMigrationsHistoryHook)
	}
}

// One returns a single eFMigrationsHistory record from the query.
func (q eFMigrationsHistoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*EFMigrationsHistory, error) {
	o := &EFMigrationsHistory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for __EFMigrationsHistory")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all EFMigrationsHistory records from the query.
func (q eFMigrationsHistoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (EFMigrationsHistorySlice, error) {
	var o []*EFMigrationsHistory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to EFMigrationsHistory slice")
	}

	if len(eFMigrationsHistoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all EFMigrationsHistory records in the query.
func (q eFMigrationsHistoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count __EFMigrationsHistory rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q eFMigrationsHistoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if __EFMigrationsHistory exists")
	}

	return count > 0, nil
}

// EFMigrationsHistories retrieves all the records using an executor.
func EFMigrationsHistories(mods ...qm.QueryMod) eFMigrationsHistoryQuery {
	mods = append(mods, qm.From("[dbo].[__EFMigrationsHistory]"))
	return eFMigrationsHistoryQuery{NewQuery(mods...)}
}

// FindEFMigrationsHistory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEFMigrationsHistory(ctx context.Context, exec boil.ContextExecutor, migrationId string, selectCols ...string) (*EFMigrationsHistory, error) {
	eFMigrationsHistoryObj := &EFMigrationsHistory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from [dbo].[__EFMigrationsHistory] where [MigrationId]=$1", sel,
	)

	q := queries.Raw(query, migrationId)

	err := q.Bind(ctx, exec, eFMigrationsHistoryObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from __EFMigrationsHistory")
	}

	if err = eFMigrationsHistoryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return eFMigrationsHistoryObj, err
	}

	return eFMigrationsHistoryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *EFMigrationsHistory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no __EFMigrationsHistory provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(eFMigrationsHistoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	eFMigrationsHistoryInsertCacheMut.RLock()
	cache, cached := eFMigrationsHistoryInsertCache[key]
	eFMigrationsHistoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			eFMigrationsHistoryAllColumns,
			eFMigrationsHistoryColumnsWithDefault,
			eFMigrationsHistoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(eFMigrationsHistoryType, eFMigrationsHistoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(eFMigrationsHistoryType, eFMigrationsHistoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO [dbo].[__EFMigrationsHistory] ([%s]) %%sVALUES (%s)%%s", strings.Join(wl, "],["), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO [dbo].[__EFMigrationsHistory] %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryOutput = fmt.Sprintf("OUTPUT INSERTED.[%s] ", strings.Join(returnColumns, "],INSERTED.["))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into __EFMigrationsHistory")
	}

	if !cached {
		eFMigrationsHistoryInsertCacheMut.Lock()
		eFMigrationsHistoryInsertCache[key] = cache
		eFMigrationsHistoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the EFMigrationsHistory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *EFMigrationsHistory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	eFMigrationsHistoryUpdateCacheMut.RLock()
	cache, cached := eFMigrationsHistoryUpdateCache[key]
	eFMigrationsHistoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			eFMigrationsHistoryAllColumns,
			eFMigrationsHistoryPrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, eFMigrationsHistoryColumnsWithAuto)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update __EFMigrationsHistory, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE [dbo].[__EFMigrationsHistory] SET %s WHERE %s",
			strmangle.SetParamNames("[", "]", 1, wl),
			strmangle.WhereClause("[", "]", len(wl)+1, eFMigrationsHistoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(eFMigrationsHistoryType, eFMigrationsHistoryMapping, append(wl, eFMigrationsHistoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update __EFMigrationsHistory row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for __EFMigrationsHistory")
	}

	if !cached {
		eFMigrationsHistoryUpdateCacheMut.Lock()
		eFMigrationsHistoryUpdateCache[key] = cache
		eFMigrationsHistoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q eFMigrationsHistoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for __EFMigrationsHistory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for __EFMigrationsHistory")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EFMigrationsHistorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), eFMigrationsHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE [dbo].[__EFMigrationsHistory] SET %s WHERE %s",
		strmangle.SetParamNames("[", "]", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, eFMigrationsHistoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in eFMigrationsHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all eFMigrationsHistory")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *EFMigrationsHistory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no __EFMigrationsHistory provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(eFMigrationsHistoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	eFMigrationsHistoryUpsertCacheMut.RLock()
	cache, cached := eFMigrationsHistoryUpsertCache[key]
	eFMigrationsHistoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			eFMigrationsHistoryAllColumns,
			eFMigrationsHistoryColumnsWithDefault,
			eFMigrationsHistoryColumnsWithoutDefault,
			nzDefaults,
		)
		insert = strmangle.SetComplement(insert, eFMigrationsHistoryColumnsWithAuto)
		for i, v := range insert {
			if strmangle.ContainsAny(eFMigrationsHistoryPrimaryKeyColumns, v) && strmangle.ContainsAny(eFMigrationsHistoryColumnsWithDefault, v) {
				insert = append(insert[:i], insert[i+1:]...)
			}
		}
		if len(insert) == 0 {
			return errors.New("models: unable to upsert __EFMigrationsHistory, could not build insert column list")
		}

		ret = strmangle.SetMerge(ret, eFMigrationsHistoryColumnsWithAuto)
		ret = strmangle.SetMerge(ret, eFMigrationsHistoryColumnsWithDefault)

		update := updateColumns.UpdateColumnSet(
			eFMigrationsHistoryAllColumns,
			eFMigrationsHistoryPrimaryKeyColumns,
		)
		update = strmangle.SetComplement(update, eFMigrationsHistoryColumnsWithAuto)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert __EFMigrationsHistory, could not build update column list")
		}

		cache.query = buildUpsertQueryMSSQL(dialect, "[dbo].[__EFMigrationsHistory]", eFMigrationsHistoryPrimaryKeyColumns, update, insert, ret)

		whitelist := make([]string, len(eFMigrationsHistoryPrimaryKeyColumns))
		copy(whitelist, eFMigrationsHistoryPrimaryKeyColumns)
		whitelist = append(whitelist, update...)
		whitelist = append(whitelist, insert...)

		cache.valueMapping, err = queries.BindMapping(eFMigrationsHistoryType, eFMigrationsHistoryMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(eFMigrationsHistoryType, eFMigrationsHistoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // MSSQL doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert __EFMigrationsHistory")
	}

	if !cached {
		eFMigrationsHistoryUpsertCacheMut.Lock()
		eFMigrationsHistoryUpsertCache[key] = cache
		eFMigrationsHistoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single EFMigrationsHistory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *EFMigrationsHistory) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no EFMigrationsHistory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), eFMigrationsHistoryPrimaryKeyMapping)
	sql := "DELETE FROM [dbo].[__EFMigrationsHistory] WHERE [MigrationId]=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from __EFMigrationsHistory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for __EFMigrationsHistory")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q eFMigrationsHistoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no eFMigrationsHistoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from __EFMigrationsHistory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for __EFMigrationsHistory")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EFMigrationsHistorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(eFMigrationsHistoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), eFMigrationsHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM [dbo].[__EFMigrationsHistory] WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, eFMigrationsHistoryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from eFMigrationsHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for __EFMigrationsHistory")
	}

	if len(eFMigrationsHistoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *EFMigrationsHistory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEFMigrationsHistory(ctx, exec, o.MigrationId)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EFMigrationsHistorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EFMigrationsHistorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), eFMigrationsHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT [dbo].[__EFMigrationsHistory].* FROM [dbo].[__EFMigrationsHistory] WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, eFMigrationsHistoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in EFMigrationsHistorySlice")
	}

	*o = slice

	return nil
}

// EFMigrationsHistoryExists checks if the EFMigrationsHistory row exists.
func EFMigrationsHistoryExists(ctx context.Context, exec boil.ContextExecutor, migrationId string) (bool, error) {
	var exists bool
	sql := "select case when exists(select top(1) 1 from [dbo].[__EFMigrationsHistory] where [MigrationId]=$1) then 1 else 0 end"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, migrationId)
	}
	row := exec.QueryRowContext(ctx, sql, migrationId)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if __EFMigrationsHistory exists")
	}

	return exists, nil
}
