// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DiagnosticCategory is an object representing the database table.
type DiagnosticCategory struct {
	ID          int    `boil:"ID" json:"ID" toml:"ID" yaml:"ID"`
	Description string `boil:"Description" json:"Description" toml:"Description" yaml:"Description"`

	R *diagnosticCategoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L diagnosticCategoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DiagnosticCategoryColumns = struct {
	ID          string
	Description string
}{
	ID:          "ID",
	Description: "Description",
}

var DiagnosticCategoryTableColumns = struct {
	ID          string
	Description string
}{
	ID:          "DiagnosticCategory.ID",
	Description: "DiagnosticCategory.Description",
}

// Generated where

var DiagnosticCategoryWhere = struct {
	ID          whereHelperint
	Description whereHelperstring
}{
	ID:          whereHelperint{field: "[dbo].[DiagnosticCategory].[ID]"},
	Description: whereHelperstring{field: "[dbo].[DiagnosticCategory].[Description]"},
}

// DiagnosticCategoryRels is where relationship names are stored.
var DiagnosticCategoryRels = struct {
}{}

// diagnosticCategoryR is where relationships are stored.
type diagnosticCategoryR struct {
}

// NewStruct creates a new relationship struct
func (*diagnosticCategoryR) NewStruct() *diagnosticCategoryR {
	return &diagnosticCategoryR{}
}

// diagnosticCategoryL is where Load methods for each relationship are stored.
type diagnosticCategoryL struct{}

var (
	diagnosticCategoryAllColumns            = []string{"ID", "Description"}
	diagnosticCategoryColumnsWithAuto       = []string{}
	diagnosticCategoryColumnsWithoutDefault = []string{}
	diagnosticCategoryColumnsWithDefault    = []string{"ID", "Description"}
	diagnosticCategoryPrimaryKeyColumns     = []string{"ID"}
)

type (
	// DiagnosticCategorySlice is an alias for a slice of pointers to DiagnosticCategory.
	// This should almost always be used instead of []DiagnosticCategory.
	DiagnosticCategorySlice []*DiagnosticCategory
	// DiagnosticCategoryHook is the signature for custom DiagnosticCategory hook methods
	DiagnosticCategoryHook func(context.Context, boil.ContextExecutor, *DiagnosticCategory) error

	diagnosticCategoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	diagnosticCategoryType                 = reflect.TypeOf(&DiagnosticCategory{})
	diagnosticCategoryMapping              = queries.MakeStructMapping(diagnosticCategoryType)
	diagnosticCategoryPrimaryKeyMapping, _ = queries.BindMapping(diagnosticCategoryType, diagnosticCategoryMapping, diagnosticCategoryPrimaryKeyColumns)
	diagnosticCategoryInsertCacheMut       sync.RWMutex
	diagnosticCategoryInsertCache          = make(map[string]insertCache)
	diagnosticCategoryUpdateCacheMut       sync.RWMutex
	diagnosticCategoryUpdateCache          = make(map[string]updateCache)
	diagnosticCategoryUpsertCacheMut       sync.RWMutex
	diagnosticCategoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var diagnosticCategoryBeforeInsertHooks []DiagnosticCategoryHook
var diagnosticCategoryBeforeUpdateHooks []DiagnosticCategoryHook
var diagnosticCategoryBeforeDeleteHooks []DiagnosticCategoryHook
var diagnosticCategoryBeforeUpsertHooks []DiagnosticCategoryHook

var diagnosticCategoryAfterInsertHooks []DiagnosticCategoryHook
var diagnosticCategoryAfterSelectHooks []DiagnosticCategoryHook
var diagnosticCategoryAfterUpdateHooks []DiagnosticCategoryHook
var diagnosticCategoryAfterDeleteHooks []DiagnosticCategoryHook
var diagnosticCategoryAfterUpsertHooks []DiagnosticCategoryHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DiagnosticCategory) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range diagnosticCategoryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DiagnosticCategory) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range diagnosticCategoryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DiagnosticCategory) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range diagnosticCategoryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DiagnosticCategory) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range diagnosticCategoryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DiagnosticCategory) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range diagnosticCategoryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DiagnosticCategory) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range diagnosticCategoryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DiagnosticCategory) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range diagnosticCategoryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DiagnosticCategory) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range diagnosticCategoryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DiagnosticCategory) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range diagnosticCategoryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDiagnosticCategoryHook registers your hook function for all future operations.
func AddDiagnosticCategoryHook(hookPoint boil.HookPoint, diagnosticCategoryHook DiagnosticCategoryHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		diagnosticCategoryBeforeInsertHooks = append(diagnosticCategoryBeforeInsertHooks, diagnosticCategoryHook)
	case boil.BeforeUpdateHook:
		diagnosticCategoryBeforeUpdateHooks = append(diagnosticCategoryBeforeUpdateHooks, diagnosticCategoryHook)
	case boil.BeforeDeleteHook:
		diagnosticCategoryBeforeDeleteHooks = append(diagnosticCategoryBeforeDeleteHooks, diagnosticCategoryHook)
	case boil.BeforeUpsertHook:
		diagnosticCategoryBeforeUpsertHooks = append(diagnosticCategoryBeforeUpsertHooks, diagnosticCategoryHook)
	case boil.AfterInsertHook:
		diagnosticCategoryAfterInsertHooks = append(diagnosticCategoryAfterInsertHooks, diagnosticCategoryHook)
	case boil.AfterSelectHook:
		diagnosticCategoryAfterSelectHooks = append(diagnosticCategoryAfterSelectHooks, diagnosticCategoryHook)
	case boil.AfterUpdateHook:
		diagnosticCategoryAfterUpdateHooks = append(diagnosticCategoryAfterUpdateHooks, diagnosticCategoryHook)
	case boil.AfterDeleteHook:
		diagnosticCategoryAfterDeleteHooks = append(diagnosticCategoryAfterDeleteHooks, diagnosticCategoryHook)
	case boil.AfterUpsertHook:
		diagnosticCategoryAfterUpsertHooks = append(diagnosticCategoryAfterUpsertHooks, diagnosticCategoryHook)
	}
}

// One returns a single diagnosticCategory record from the query.
func (q diagnosticCategoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DiagnosticCategory, error) {
	o := &DiagnosticCategory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for DiagnosticCategory")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DiagnosticCategory records from the query.
func (q diagnosticCategoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (DiagnosticCategorySlice, error) {
	var o []*DiagnosticCategory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DiagnosticCategory slice")
	}

	if len(diagnosticCategoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DiagnosticCategory records in the query.
func (q diagnosticCategoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count DiagnosticCategory rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q diagnosticCategoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if DiagnosticCategory exists")
	}

	return count > 0, nil
}

// DiagnosticCategories retrieves all the records using an executor.
func DiagnosticCategories(mods ...qm.QueryMod) diagnosticCategoryQuery {
	mods = append(mods, qm.From("[dbo].[DiagnosticCategory]"))
	return diagnosticCategoryQuery{NewQuery(mods...)}
}

// FindDiagnosticCategory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDiagnosticCategory(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*DiagnosticCategory, error) {
	diagnosticCategoryObj := &DiagnosticCategory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from [dbo].[DiagnosticCategory] where [ID]=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, diagnosticCategoryObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from DiagnosticCategory")
	}

	if err = diagnosticCategoryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return diagnosticCategoryObj, err
	}

	return diagnosticCategoryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DiagnosticCategory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no DiagnosticCategory provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(diagnosticCategoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	diagnosticCategoryInsertCacheMut.RLock()
	cache, cached := diagnosticCategoryInsertCache[key]
	diagnosticCategoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			diagnosticCategoryAllColumns,
			diagnosticCategoryColumnsWithDefault,
			diagnosticCategoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(diagnosticCategoryType, diagnosticCategoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(diagnosticCategoryType, diagnosticCategoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO [dbo].[DiagnosticCategory] ([%s]) %%sVALUES (%s)%%s", strings.Join(wl, "],["), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO [dbo].[DiagnosticCategory] %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryOutput = fmt.Sprintf("OUTPUT INSERTED.[%s] ", strings.Join(returnColumns, "],INSERTED.["))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into DiagnosticCategory")
	}

	if !cached {
		diagnosticCategoryInsertCacheMut.Lock()
		diagnosticCategoryInsertCache[key] = cache
		diagnosticCategoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DiagnosticCategory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DiagnosticCategory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	diagnosticCategoryUpdateCacheMut.RLock()
	cache, cached := diagnosticCategoryUpdateCache[key]
	diagnosticCategoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			diagnosticCategoryAllColumns,
			diagnosticCategoryPrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, diagnosticCategoryColumnsWithAuto)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update DiagnosticCategory, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE [dbo].[DiagnosticCategory] SET %s WHERE %s",
			strmangle.SetParamNames("[", "]", 1, wl),
			strmangle.WhereClause("[", "]", len(wl)+1, diagnosticCategoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(diagnosticCategoryType, diagnosticCategoryMapping, append(wl, diagnosticCategoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update DiagnosticCategory row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for DiagnosticCategory")
	}

	if !cached {
		diagnosticCategoryUpdateCacheMut.Lock()
		diagnosticCategoryUpdateCache[key] = cache
		diagnosticCategoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q diagnosticCategoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for DiagnosticCategory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for DiagnosticCategory")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DiagnosticCategorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), diagnosticCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE [dbo].[DiagnosticCategory] SET %s WHERE %s",
		strmangle.SetParamNames("[", "]", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, diagnosticCategoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in diagnosticCategory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all diagnosticCategory")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *DiagnosticCategory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no DiagnosticCategory provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(diagnosticCategoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	diagnosticCategoryUpsertCacheMut.RLock()
	cache, cached := diagnosticCategoryUpsertCache[key]
	diagnosticCategoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			diagnosticCategoryAllColumns,
			diagnosticCategoryColumnsWithDefault,
			diagnosticCategoryColumnsWithoutDefault,
			nzDefaults,
		)
		insert = strmangle.SetComplement(insert, diagnosticCategoryColumnsWithAuto)
		for i, v := range insert {
			if strmangle.ContainsAny(diagnosticCategoryPrimaryKeyColumns, v) && strmangle.ContainsAny(diagnosticCategoryColumnsWithDefault, v) {
				insert = append(insert[:i], insert[i+1:]...)
			}
		}
		if len(insert) == 0 {
			return errors.New("models: unable to upsert DiagnosticCategory, could not build insert column list")
		}

		ret = strmangle.SetMerge(ret, diagnosticCategoryColumnsWithAuto)
		ret = strmangle.SetMerge(ret, diagnosticCategoryColumnsWithDefault)

		update := updateColumns.UpdateColumnSet(
			diagnosticCategoryAllColumns,
			diagnosticCategoryPrimaryKeyColumns,
		)
		update = strmangle.SetComplement(update, diagnosticCategoryColumnsWithAuto)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert DiagnosticCategory, could not build update column list")
		}

		cache.query = buildUpsertQueryMSSQL(dialect, "[dbo].[DiagnosticCategory]", diagnosticCategoryPrimaryKeyColumns, update, insert, ret)

		whitelist := make([]string, len(diagnosticCategoryPrimaryKeyColumns))
		copy(whitelist, diagnosticCategoryPrimaryKeyColumns)
		whitelist = append(whitelist, update...)
		whitelist = append(whitelist, insert...)

		cache.valueMapping, err = queries.BindMapping(diagnosticCategoryType, diagnosticCategoryMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(diagnosticCategoryType, diagnosticCategoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // MSSQL doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert DiagnosticCategory")
	}

	if !cached {
		diagnosticCategoryUpsertCacheMut.Lock()
		diagnosticCategoryUpsertCache[key] = cache
		diagnosticCategoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DiagnosticCategory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DiagnosticCategory) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DiagnosticCategory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), diagnosticCategoryPrimaryKeyMapping)
	sql := "DELETE FROM [dbo].[DiagnosticCategory] WHERE [ID]=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from DiagnosticCategory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for DiagnosticCategory")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q diagnosticCategoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no diagnosticCategoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from DiagnosticCategory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for DiagnosticCategory")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DiagnosticCategorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(diagnosticCategoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), diagnosticCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM [dbo].[DiagnosticCategory] WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, diagnosticCategoryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from diagnosticCategory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for DiagnosticCategory")
	}

	if len(diagnosticCategoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DiagnosticCategory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDiagnosticCategory(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DiagnosticCategorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DiagnosticCategorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), diagnosticCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT [dbo].[DiagnosticCategory].* FROM [dbo].[DiagnosticCategory] WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, diagnosticCategoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DiagnosticCategorySlice")
	}

	*o = slice

	return nil
}

// DiagnosticCategoryExists checks if the DiagnosticCategory row exists.
func DiagnosticCategoryExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select case when exists(select top(1) 1 from [dbo].[DiagnosticCategory] where [ID]=$1) then 1 else 0 end"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if DiagnosticCategory exists")
	}

	return exists, nil
}
